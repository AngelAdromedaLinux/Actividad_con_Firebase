import React, { Component } from "react";
import { Text, StyleSheet, View } from 'react-native';

export default function Home() {

    return (
        <View>
        <Text> has accedido a home </Text>
        </View>
    );

}

const styles = StyleSheet.create({})